{
  "name": "rock-forge",
  "private": true,
  "version": "0.1.0",
  "description": "Procedural rock geometry that scales: one shared topology, per-rock shape baked to a texture, displaced on the GPU.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node tools/shape-test.mjs && node tools/winding-check.mjs && node tools/shader-check.mjs && node tools/packing-check.mjs && node tools/collision-test.mjs",
    "test:shape": "node tools/shape-test.mjs",
    "test:collision": "node tools/collision-test.mjs",
    "test:winding": "node tools/winding-check.mjs",
    "test:packing": "node tools/packing-check.mjs",
    "test:shader": "node tools/shader-check.mjs"
  },
  "license": "ISC",
  "dependencies": {
    "@babylonjs/core": "^8.56.2"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  }
}
